#include "Character.h"

// Destructor
Character::~Character() {}

void Character::healthCheck() {
    if (health > 0) {
        int i = 0;
        // placeholder for death checks. might migrate this
    }
}
